# dependencies
node_modules
.bun
/.pnp
.pnp.js
.npmrc

# We use bun so these files shouldn't even exist. But if they do (e.g., because eslint runs on node by default), we don't want them commited.
package-lock.json

# cache
temp/
.eslintcache
vite.config.ts.timestamp*

# production
dist
*.tsbuildinfo

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

/.vscode
